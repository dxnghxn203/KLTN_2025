name: Build Tracking API (Test)

on:
  push:
    paths:
      - '.github/workflows/build-tracking-api.yml'
  workflow_dispatch:

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/tracking-api-kltn2025
  API_DIR: ./tracking-manager/packages/tracking-api
  VAULT_ADDR: ${{ secrets.VAULT_ADDR }}
  VAULT_SECRET_PATH: secret/production/.env

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Import DB Secrets from Vault
        id: vault-secrets-db
        uses: hashicorp/vault-action@v3
        with:
          url: ${{ env.VAULT_ADDR }}
          method: approle
          roleId: ${{ secrets.VAULT_ROLE_ID }}
          secretId: ${{ secrets.VAULT_SECRET_ID }}
          secrets: |
            MONGO_HOST secret/production/.env MONGO_HOST
            RABBITMQ_USER secret/production/.env RABBITMQ_USER
            RABBITMQ_PW secret/production/.env RABBITMQ_PW
            RABBITMQ_HOST secret/production/.env RABBITMQ_HOST
            RABBITMQ_PORT secret/production/.env RABBITMQ_PORT
            RABBITMQ_VHOST secret/production/.env RABBITMQ_VHOST
            REDIS_HOST secret/production/.env REDIS_HOST
            REDIS_PORT secret/production/.env REDIS_PORT
            REDIS_PASSWORD secret/production/.env REDIS_PASSWORD

      - name: Import API Secrets from Vault
        id: vault-secrets-api
        uses: hashicorp/vault-action@v3
        with:
          url: ${{ env.VAULT_ADDR }}
          method: approle
          roleId: ${{ secrets.VAULT_ROLE_ID }}
          secretId: ${{ secrets.VAULT_SECRET_ID }}
          secrets: |
            SUPABASE_PASS secret/production/.env SUPABASE_PASS
            SUPABASE_key secret/production/.env SUPABASE_key
            SUPABSE_URL secret/production/.env SUPABSE_URL
            API_MONGO_USER secret/production/.env API_MONGO_USER
            API_MONGO_PWS secret/production/.env API_MONGO_PWS
            API_MONGO_DB secret/production/.env API_MONGO_DB
            ES_HOST secret/production/.env ES_HOST
            ES_PORT secret/production/.env ES_PORT
            ES_USER secret/production/.env ES_USER

      - name: Import Other Secrets from Vault
        id: vault-secrets-other
        uses: hashicorp/vault-action@v3
        with:
          url: ${{ env.VAULT_ADDR }}
          method: approle
          roleId: ${{ secrets.VAULT_ROLE_ID }}
          secretId: ${{ secrets.VAULT_SECRET_ID }}
          secrets: |
            ES_PW secret/production/.env ES_PW
            PAYMENT_API_URL secret/production/.env PAYMENT_API_URL
            GMAIL_PASS secret/production/.env GMAIL_PASS
            GMAIL_USER secret/production/.env GMAIL_USER
            JWT_PRIVATE_KEY secret/production/.env JWT_PRIVATE_KEY
            JWT_PUBLIC_KEY secret/production/.env JWT_PUBLIC_KEY
            ALGORITHM secret/production/.env ALGORITHM
            API_PORT secret/production/.env API_PORT

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ${{ env.API_DIR }}
          file: ${{ env.API_DIR }}/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
          build-args: |
            MONGO_HOST=${{ env.MONGO_HOST }}
            RABBITMQ_USER=${{ env.RABBITMQ_USER }}
            RABBITMQ_PW=${{ env.RABBITMQ_PW }}
            RABBITMQ_HOST=${{ env.RABBITMQ_HOST }}
            RABBITMQ_PORT=${{ env.RABBITMQ_PORT }}
            RABBITMQ_VHOST=${{ env.RABBITMQ_VHOST }}
            REDIS_HOST=${{ env.REDIS_HOST }}
            REDIS_PORT=${{ env.REDIS_PORT }}
            REDIS_PASSWORD=${{ env.REDIS_PASSWORD }}
            SUPABASE_PASS=${{ env.SUPABASE_PASS }}
            SUPABASE_key=${{ env.SUPABASE_key }}
            SUPABSE_URL=${{ env.SUPABSE_URL }}
            API_MONGO_USER=${{ env.API_MONGO_USER }}
            API_MONGO_PWS=${{ env.API_MONGO_PWS }}
            API_MONGO_DB=${{ env.API_MONGO_DB }}
            ES_HOST=${{ env.ES_HOST }}
            ES_PORT=${{ env.ES_PORT }}
            ES_USER=${{ env.ES_USER }}
            ES_PW=${{ env.ES_PW }}
            PAYMENT_API_URL=${{ env.PAYMENT_API_URL }}
            GMAIL_PASS=${{ env.GMAIL_PASS }}
            GMAIL_USER=${{ env.GMAIL_USER }}
            JWT_PRIVATE_KEY=${{ env.JWT_PRIVATE_KEY }}
            JWT_PUBLIC_KEY=${{ env.JWT_PUBLIC_KEY }}
            ALGORITHM=${{ env.ALGORITHM }}
            API_PORT=${{ env.API_PORT }}
          cache-from: type=gha
          cache-to: type=gha,mode=max