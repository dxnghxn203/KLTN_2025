cluster.name: "tracking-cluster"
node.name: "node-1"
network.host: 0.0.0.0

# Cấu hình discovery
discovery.type: single-node

# Giới hạn bộ nhớ
bootstrap.memory_lock: true

# Các cấu hình xpack đã bị xóa vì elasticsearch-oss không hỗ trợ
