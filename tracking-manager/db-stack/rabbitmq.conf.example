# Cấu hình đơn giản hóa để tránh lỗi cú pháp
vm_memory_high_watermark.absolute = 75MB

# Giảm số lượng kết nối
connection_max = 50
channel_max = 100

# Cài đặt log cơ bản
log.file.level = warning

# Tắt thống kê cơ bản
collect_statistics_interval = 120000
