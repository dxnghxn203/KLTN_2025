'use client';
import { useState } from 'react';

const Dashboard = () => {
    const [count, setCount] = useState(0);
    return (
        <div>
            <h1>Dashboard</h1>
        </div>
    );
}

export default Dashboard;